---
# 1) Install / bootstrap microk8s
- import_playbook: microk8s_install.yml

# 2) Apply core manifests (namespace, db/minio/mailhog/backend/frontend/ingress + waits)
- import_playbook: k8s_apply_core.yml

# 3) Seed DB (Job 15 + sanity checks)
- import_playbook: k8s_seed_db.yml

# 4) Load photos (Job 16 + sanity checks)
- import_playbook: k8s_load_photos.yml
